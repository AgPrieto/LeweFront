const initialState = {}

function reducer(state = initialState, action) {
  switch (action.type) {
    // Aquí puedes definir tus acciones
    default:
      return state
  }
}

export default reducer
